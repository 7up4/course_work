= form_for @tariff_zone do |f|
  - if @tariff_zone.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tariff_zone.errors.count, "error")} prohibited this tariff_zone from being saved:"
      %ul
        - @tariff_zone.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
