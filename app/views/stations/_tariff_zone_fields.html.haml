.nested-fields#tariff_zone
  .row
    .col-md-5
      .form-group
        = f.label :name
        = f.text_field :name, {class: "form-control"}
  .form-actions
    = link_to_remove_association f, class: 'btn btn-danger' do
      = fa_icon :times
      = t('cocoon.tariff_zones.remove')
